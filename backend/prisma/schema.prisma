// Prisma Schema Configuration - Phase 1 Initial Setup
// Application models (User, Customer, Product, StockMovement, Challan, ChallanItem) will be added in Phase 2.

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
